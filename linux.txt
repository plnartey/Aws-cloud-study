cd command takes you into a new directory(cd downloads)
touch comand is used to create a file ($ touch my_documents.txt)
mkdri means "make directory". it is use to create a new directory or let's say a new folder. (mkdir my_pictures)
The move command moves a file or directory from one location to another. You can use it to rename a file or to move it to a different directory altogether. Your working directory can't be the directory you're moving. (for renaming: "mv done.txt complete.txt"). Moving a file from the current directory to another nested directory:(mv some_file.txt some_directory/some_file.txt).Moving a file from the current directory, to the parent directory:("mv some_file.txt ../some_file.txt").If you don't want to rename the file and you're just moving it to a different directory, you can omit the filename:("mv some_file.txt some_directory/").
